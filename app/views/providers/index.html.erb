<h1>Empresas registradas (Proveedores)</h1>
 <table>
  <tr>
    <th></th>
	<th>Nombre comercial</th>
    <th>Contacto</th>
	<th>Email</th>
	<th>Password</th>
  </tr>
 <% i= 1 %>
  <% @providers.each do |provider| %>
    <tr>
	  <td><%= i %></td>
      <td><%= provider.nombre_comercial %></td>
      <td><%= provider.nombre_contacto %></td>
	  <td><%= provider.email %><td>
	  <td><%= provider.password %><td>
      <td><%= link_to 'Mostrar', provider_path(provider) %></td>
      <td><%= link_to 'Editar', edit_provider_path(provider) %></td>
	</tr>
	<% i= i + 1 %>
  <% end %>
</table>
<%= link_to :Registrar_nueva_empresa, new_provider_path %>
<br>
<%= link_to :Indice, controller: 'welcome' %>